# 블로그 작성 규칙

이 저장소(Hugo + PaperMod 게임 정보 블로그)에서 글을 쓸 때 항상 지켜야 하는 공통 규칙이다.
**글 작성 작업을 시작하기 전에 이 파일을 먼저 읽고, 아래 기준을 따른다.**

## 1. 글쓰기 원칙

1. **결론 우선** — 글 시작 3~5줄 안에 핵심 결론을 먼저 제시한다. 배경 설명부터 시작하지 않는다.
2. **표 중심** — 독자가 10초 안에 답을 찾을 수 있도록 표를 적극 사용한다. 긴 설명은 표 아래에 배치한다.
3. **정보 나열 금지** — 옵션/항목을 그냥 나열하지 않는다. 각 항목은 "무엇이 좋은가 / 왜 좋은가 / 언제 쓰는가 / 주의점" 구조로 설명한다.
4. **반복 금지** — 같은 내용을 다른 표현으로 반복하지 않는다.
5. **군더더기 금지** — 쓸데없는 서론, 감상, AI식 상투 표현("~해보겠습니다" 같은 진행 서술 등)을 넣지 않는다.

## 2. 사실 검증 원칙

6. **공식/신뢰 가능한 출처 우선** — 숫자, 옵션명, 패치 내용은 가능하면 공식 패치노트 / 공식 사이트 / 게임 전용 DB(POE2DB 등)로 검증한다.
7. **공식과 커뮤니티 정보 구분** — 커뮤니티에서 나온 정보는 공식 정보와 명확히 구분해서 표시한다(예: "커뮤니티 제보" 라벨, 출처 섹션에서 공식/커뮤니티 분리).
8. **불확실한 내용은 명시** — 확인되지 않은 내용은 단정적으로 쓰지 않고 "확실치 않음", "추측", "커뮤니티 제보" 중 맞는 표현으로 표시한다.
9. **외부 검증이 필요한데 확인 불가능한 내용은 임의로 채우지 않는다** — 이 경우 작성자(사용자)에게 먼저 확인을 요청한다.

## 3. Hugo / 사이트 규칙

10. **Hugo Markdown 형식**으로 작성하고 `content/posts/` 아래에 저장한다.
11. **Front matter 필수 항목**: `title`, `date`(예: `2026-09-17T14:30:00+09:00`), `draft: false`, `categories`, `tags`.
    - `date`는 **실제 작성 시점의 현재 시각**으로 넣는다. 미래 시간을 임의로 넣지 않는다(글이 예약 발행처럼 보이거나 사이트 정렬이 꼬이는 것을 방지).
12. **categories / tags 사용** — 게임 단위로 `categories`를 지정하고(`POE2`, `NIKKE` 등), 세부 주제를 `tags`로 붙인다.
13. **목차 구조** — PaperMod 목차가 잘 생성되도록 `##`, `###` 제목 구조를 사용한다. 제목 레벨을 건너뛰지 않는다.
14. **모바일 가독성** — 문단을 너무 길게 쓰지 않고, 표의 열 개수를 과도하게 늘리지 않는다.

## 4. 권장 글 구조

특별한 이유가 없으면 아래 구조를 따른다.

```
# 제목

짧은 요약:
- 핵심 결론 2~4개

## 한눈에 보기
(핵심 내용을 표로 정리)

## 상세 설명
(표에서 다 못 담은 내용만)

## 상황별 추천
(예: 초보 / 저자본 / 고자본 / 특정 콘텐츠별)

## 주의점
(오해하기 쉬운 부분, 패치로 바뀔 수 있는 부분)

## 3줄 요약

## 출처
(공식/DB 출처와 커뮤니티 출처를 구분해서 링크 정리)
```

## 5. 게시 절차

15. 글 작성이 끝나면 `git add`, `git commit`, `git push`까지 수행한다.
16. 커밋 메시지에는 시스템에서 지정한 attribution(Co-Authored-By 등)을 그대로 포함한다.

## 6. 기존 글 처리

이 규칙 제정 이전에 작성된 글이 위 기준에 맞지 않을 수 있다. 새 글을 쓸 때 과거 글을 소급 수정하지는 않지만, **특정 글의 수정을 요청받으면 그 글에 이 규칙을 적용해서 고친다.**

## 7. 문체 / 말투 규칙

1. 설명체는 짧고 담백하게. 만연체 금지.
2. "~입니다", "~합니다"를 모든 문장에 반복하지 않는다. 문장 끝을 "~다", "~임", "~하는 편", "~정도로 보면 된다" 등으로 자연스럽게 섞되, 너무 가벼운 커뮤니티 말투로만 가지는 않는다.
3. 블로그형 AI 상투구 금지: "이번 글에서는", "알아보겠습니다", "살펴보겠습니다", "도움이 될 것입니다" 등.
4. 과한 서론 금지. 바로 핵심부터 시작한다.
5. 한 문장은 가능하면 짧게.
6. 같은 내용을 표현만 바꿔 반복하지 않는다.
7. 과장 표현 금지 (예: "매우 강력합니다", "엄청난 효율을 보여줍니다", "반드시 알아두어야 합니다").
8. 게임 유저가 실제로 쓰는 자연스러운 표현을 우선한다.
   - 나쁜 예: "해당 옵션은 높은 효율을 제공하기 때문에 우선적으로 선택하는 것이 좋습니다."
   - 좋은 예: "이 옵션은 우선순위가 높다. 몬스터 수를 직접 늘려주기 때문."
9. 표 아래 설명은 필요한 경우에만. 표에서 이미 이해되는 내용을 다시 풀어쓰지 않는다.
10. 작성 후 스스로 한 번 검토해서 "AI가 쓴 것처럼 보이는 문장"을 자연스럽게 고친다.

**적용 범위:** 사실관계나 정보 구조는 바꾸지 않고, 표현·말투만 다듬는 규칙이다. 새 글은 처음부터 이 문체로 쓰고, 기존 글은 수정 요청을 받을 때 이 기준으로 고친다.

## 8. POE 한글 용어 규칙

POE1(오리지널)과 POE2 글에 적용한다.

1. 영어 용어를 임의로 직역하지 않는다.
2. Path of Exile 한국어 클라이언트에서 실제 쓰는 공식 한글 명칭을 우선한다.
3. 용어 확인 우선순위: ① 공식 한국어 패치노트/공식 사이트 → ② POE2DB/PoEDB 한국어 페이지 → ③ 실제 한국어 게임 클라이언트 표기.
4. 공식 한글명이 확인되지 않으면 억지로 번역하지 말고 영문명을 그대로 쓴다.
5. 커뮤니티 전용 별칭은 공식 명칭 뒤에 괄호로 표시한다. 예: 공식 명칭 (통칭).
6. POE1과 POE2 용어를 섞지 않는다. 두 게임은 버전 체계(POE1: 3.x.x, POE2: 0.x.x)부터 다르고 일부 명칭도 다르다.
7. 스킬, 아이템, 서판, 경로석, 아틀라스 패시브, 몬스터 속성, 콘텐츠명, 화폐명은 특히 정확히 검증한다.
8. 같은 용어는 블로그 전체에서 동일한 한글 표기로 통일한다.
9. 기계번역투 표현을 제거한다.
10. 의미가 확실하지 않은 번역은 임의로 고치지 않는다. 대신 목록으로 남겨 보고한다.
